# Local tooling & editor config
.claude/
.vscode/

# Temporary / working files
temporary screenshots/
*.log

# Local-only notes (kept on disk, not published)
CLAUDE (1).md

# Unoptimised source images — the web-sized .jpg next to each is what ships
assets/pyramid/*.png
assets/brand-character/soeke-collection.png
assets/brand-character/isometric-soeke-city.png
assets/logogenerator/*.png

# Dependencies (if any tooling is added later)
node_modules/

# OS cruft
Thumbs.db
Desktop.ini
.DS_Store
